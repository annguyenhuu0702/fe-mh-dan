type ProblemDto = {
  adminUserId?: number;
  departmentId?: number;
  title: string;
  industry: string;
  contact: string;
  status: string;
  processingDate: string;
  note: string;
};

export type { ProblemDto };
